{% for upstream in nginx_upstreams %}
upstream {{ upstream.name }} {
{% for host in upstream.hosts %}
    server {{ host.endpoint }} {{ host.options}};
{% endfor %}
}
{% endfor %}

map $http_upgrade $connection_upgrade {
        default upgrade;
        '' close;
}
